#include "holberton.h"

/**
 * _abs - prints the value of a interger.
 *@a: value number.
 * Return: Always 0.
 */


int _abs(int a)
{
if (a >= 0)
{
return (a);
}
else
{
return (0 - a);
}
return (0);
}
