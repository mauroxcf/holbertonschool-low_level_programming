#include "holberton.h"

/**
 * _isalpha - checks for a alpha character.
 *@c: letter of the alphabet.
 * Return: Always 0.
 */


int _isalpha(int c)
{

if (c >= 0 && c <= 127)
{
return (1);
}
else
{
return (0);
}
}
