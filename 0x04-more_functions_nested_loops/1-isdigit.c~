#include "holberton.h"


/**
 *_isdigit - check the code for Holberton School students.
 * @c: the variable
 * Return: 1 if the variable its uppercase.
 */


int _isdigit(int c)
{

if (c >= 30 && c <= 39)
{
return (1);
}
else

return (0);
}
