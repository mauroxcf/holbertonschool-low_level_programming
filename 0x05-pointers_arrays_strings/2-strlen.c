#include "holberton.h"

/**
 * _strlen - calculates the length of the string.
 * @s:pointer variable.
 * Return: Always 0.
 */

int _strlen(char *s)
{

int a;

for (a = 0; s[a] != '\0'; a++)
{
}
return (a);


}
