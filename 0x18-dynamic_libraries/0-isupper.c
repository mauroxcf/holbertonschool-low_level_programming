#include "holberton.h"


/**
 *_isupper - check the code for Holberton School students.
 * @c: the variable
 * Return: 1 if the variable its uppercase.
 */


int _isupper(int c)
{

if (c >= 'A' && c <= 'Z')
{
return (1);
}
else

return (0);
}
