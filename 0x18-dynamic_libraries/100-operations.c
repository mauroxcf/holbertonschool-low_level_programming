#include <stdio.h>
#include "holberton.h"

int mod(int a, int b)
{
	int result = 1;
	
	return (result = a % b);
}

int add(int a, int b)
{
	float result;

	return (result = a + b);
}

int sub(int a, int b)
{
	float result;

	return (result = a - b);
}

int mul(int a, int b)
{
	float result;

	return (result = a * b);
}

int div(int a, int b)
{
	int result;

	return (result = a / b);
}
