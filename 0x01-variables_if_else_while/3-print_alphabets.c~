#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

char b;
char c;

for (b = 'a'; b <= 'z'; b++)
{
putchar (b);
b++;
}

for (c = 'A'; c <= 'Z'; c++)
{
putchar (c);
c++;
}
putchar ('\n');

return (0);

}
