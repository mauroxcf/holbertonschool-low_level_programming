#include <stdio.h>

/**
 * main - Entry point
 *
 * Return: Always 0 (Success)
 */

int main(void)
{

char a;
a = 'a';

do {

putchar (a);
a++;

while (a <= 'z')
{
if (a == 'q' || a == 'e')
}
putchar ('\n');
return (0);
}
